# 自定义指令：

## /todo 指令
- 收到以 `/todo <内容>` 开头的消息时：将内容作为待办项追加到项目 docs/todo.md。自动判断最合适的章节位置，必要时新建小节，补齐 - [ ] 子项格式和简要说明；改完后告诉用户写入的章节与行号，无需额外确认。
- 收到 `/todo-check` 时：读取 docs/todo.md，按章节分组输出所有未勾选（- [ ]）的待办清单。
